<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD MAPPER 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abacorp.aba.model.Overlay">

    <!-- Mapper -->
    <resultMap id="Overlay" type="Overlay" />

    <select id="findOverlays" resultMap="Overlay" parameterType="MapFiltersDto">
        SELECT id, center_x, center_y, name, count, weight
        FROM overlays WHERE (weight = #{weight})
        <if test="#{east} != null">
            AND (center_x BETWEEN #{west} AND #{east})
            AND (center_y BETWEEN #{south} AND #{north})
        </if>
    </select>
</mapper>
