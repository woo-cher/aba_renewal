<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD MAPPER 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abacorp.aba.model.Post">

    <!-- Mapper -->
    <resultMap id="post" type="post" />

    <select id="createPost" parameterType="post">
        INSERT INTO posts (register, title, contents, created_at, updated_at, type)
        VALUES (#{register.userId}, #{title}, #{contents}, current_timestamp, current_timestamp, #{postType})
    </select>
</mapper>
